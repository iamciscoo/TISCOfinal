'use client'

/**
 * This hook is no longer needed since cart management is now client-side only
 * Keeping as a stub to prevent import errors during cleanup
 */
export function useRealtimeCart() {
  // No-op: cart is now purely client-side with local storage persistence
}
